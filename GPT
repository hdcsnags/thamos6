PRIMARY GOAL

Stop using placeholder “card-only” logic. Reuse the existing working API wrappers in src/lib/threatIntel.ts (scanURL, lookupHash, etc.) and render real results in the new V2 result pages.

1) FIX HASH: HashResult.tsx must call the real API (no placeholder)

File: src/pages/results/HashResult.tsx

Current problem: It renders “Hash lookup coming soon” and never calls lookupHash(). It uses a fake timer and hard-coded sources.

Required fix:

Import and call: lookupHash(hash) from src/lib/threatIntel.ts

Replace the fake timeout with real request logic like URLResult.tsx does.

When request returns:

Populate RawJsonCollapse with the full API response (not {hash, hashType}).

Render Evidence/SourceCards from the returned sources/results.

Normalize hash results like URL normalization
Your hash response uses inconsistent source keys (virustotal_hash, hybrid_analysis, etc.) while the UI expects nicer keys and display names.

Implement a simple normalization in either:

lookupHash() (preferred), OR

HashResult.tsx before rendering

Normalization rules:

Canonical container: normalizedResults (frontend-friendly)

Accept both data.sources and data.results

Map keys:

virustotal_hash → virustotal

hybrid_analysis → hybridanalysis

alienvault → otx

Each normalized source must be:

{
  found: boolean,
  malicious: boolean,
  details: object,
  error?: string,
  threatScore?: number
}

Keep original under rawSources/rawResults for debugging.

Update display names
Extend getSourceDisplayName() in src/lib/threatIntel.ts to also recognize:

virustotal_hash, hybrid_analysis, alienvault
so the UI never shows raw backend key names.

Acceptance test
Scanning the same SHA256 from the screenshot must render:

VT detection ratio, file metadata if available

MalwareBazaar/Hybrid/OTX if enabled

Raw JSON shows the real returned payload

2) FIX URL + UNIVERSAL SCANNER: ensure it calls scanURL() and routes to URLResult
The real bug you’re seeing:

Bolt built the “Universal IOC Scanner” landing but didn’t wire it to the real scan functions. It likely routes to result pages without calling your wrappers or it builds empty cards.

Required fix:

Universal scanner submit handler must:

Detect IOC type (IP / URL / domain / hash / extension)

Navigate to result route

Result route page must perform the request using the existing wrapper:

URL → scanURL(url)

hash → lookupHash(hash)

IP → lookupIP(ip)

etc.

Strict rule: No new fetch() logic in pages. Always use src/lib/threatIntel.ts

3) UX CHANGE: Remove “Universal IOC Scanner” hero section (trim the landing page)

On the Scanner landing page (your screenshot 2):

Remove:

the large icon

“Universal IOC Scanner” big heading

the descriptive paragraph block

Keep only:

the input

“Scan Now” button

(optional) small “What we check” grid below, but tighter (no huge padding)

Goal: compact, premium, not “landing page marketing.”

4) ADD PERSISTENT TOP SEARCH BAR (VirusTotal-style)
Requirement

Like VirusTotal, the user must be able to scan something new from anywhere (results pages included) without “going back.”

Implementation

Add a small persistent search bar in the top header near the logo (all pages).

It should accept any IOC and on Enter/Scan:

run the same detection

route to the appropriate results page

Nice-to-have

When you’re already on a results page, the top bar stays visible and doesn’t push content down (compact height).

5) LIGHT MODE COLOR TWEAKS (make it feel less generic)

Your light mode screenshot feels “default Tailwind + soft blue button.” Make it look more “security console.”

Adjustments

Light background: slightly cooler neutral (not pure white), with stronger card separation.

Primary accent: keep cyan/blue but reduce gradient “toy” feel:

make the primary button more “solid enterprise” (one gradient at most)

Improve contrast:

headings darker

secondary text slightly darker than current

borders slightly more defined in light mode

Rule: Don’t change the design system wildly — just tighten contrast + accent consistency.