Rename + reposition: “Threat Intel” as a first-class module

Instead of “News Feed” with two buttons, make the nav item Threat Intel and inside it you have:

Intel Stream (news + advisories + your watchlist matches)

Ransomware Tracker

Intel Digest (daily/weekly summary)

Watchlists (entities + IOCs)

Saved / Cases (your triage work)

This instantly turns it from “RSS page” into “Intel Center”.

2) Change the core layout to feel premium: “List + Preview” (like Outlook / Sentinel)

Right now it’s a vertical stack of cards. That’s fine, but premium tools do this:

Left: stream list (fast scroll, dense, filterable)
Right: preview panel (full article, extracted IOCs, actions)

Why it’s elite: you stop bouncing between “open article” and “back”. You triage at speed.

Add top bar actions:

Search

Filters

Sort (Newest / Most relevant / Most severe)

“Mark reviewed”, “Save”, “Create case”, “Add to watchlist”

3) Make the stream “intel-aware”, not “RSS-aware”
A. Fix the CDATA ugliness

Your SANS item showing <![CDATA[ ... ]]> is the exact kind of “vibe-coded” blemish that kills premium feel.
Fix by stripping CDATA + decoding entities before render.

B. Enrich every item automatically (lightweight)

For each article:

Extract IOCs (IPs, domains, URLs, hashes, CVEs)

Show them as clickable chips

Clicking a chip runs your existing lookups (IP/Domain/Hash) in a side drawer

That one move makes the page feel like a threat intel platform, not a feed reader.

4) Add “Signal” scoring so the feed isn’t noisy

Give each item a simple score badge:

Impact (ransomware? auth bypass? 0-day? education sector?)

Proximity (matches DSBN keywords, tech stack: M365, Fortinet, Netskope, Google, etc.)

Confidence (source trust + cross-source duplication)

Then allow sorting by Top Signals.

Even basic scoring makes it feel “expensive”.

5) Tighten the taxonomy: Vulnerabilities / Alerts / Threats / News → “Intel Lenses”

Your tabs are good — make them feel more purposeful:

Vulns (CVE focused, EPSS/CVSS, exploit status)

Campaigns (phishing kits, AITM, current waves)

Ransomware (your existing tracker)

Education (curated lens: schoolboard-relevant)

My Watchlist (always personal)

This makes it feel tailored, not generic.

6) Turn “Watchlist” into a killer feature

Premium move: watchlist isn’t just saved articles — it’s entities.

Watchlist types:

Org names (DSBN vendors, local municipalities)

Domains (your brands)

Threat actors (“Scattered Spider”, “LockBit”, etc.)

Keywords (“MFA fatigue”, “AiTM”, “OAuth consent phishing”)

When matched:

highlight articles

generate an alert card

optionally email/teams notification later

7) Ransomware page: it’s already strong — add a “Details Drawer” + filters

It already looks premium. Make it operate premium:

Filter pills: Sector, Country, Actor, Last 7/30/90 days

Click a victim → right-side drawer with:

actor profile

timeline

leak site link (safe open)

related victims

tags + notes

This turns it from “dashboard” to “analyst workflow.”

8) One premium cherry: “Daily Intel Digest”

A small card at the top:

“Today’s Intel Digest”

5 bullets summary

3 top vulns to watch

2 active campaigns

1 ransomware highlight

Even if it’s simple at first, it screams “enterprise product.”