Edge Function: Add /domain route + domain checks

File: supabase/functions/threat-intel/index.ts (your router)

Add a VirusTotal domain helper (parallel to existing checkVirusTotalURL + checkVirusTotalHash):

New function: checkVirusTotalDomain(ctx, domain, apiKey)

Call VT v3: GET https://www.virustotal.com/api/v3/domains/${domain}

Return a ThreatResult:

source: "virustotal" (IMPORTANT: use canonical key “virustotal”, not “virustotal_domain”)

isMalicious: based on VT stats / reputation signal

threatScore: derived (ex: malicious>0 → 80, suspicious>0 → 50, else low)

data: raw VT payload or a reduced payload

Add a WHOIS/RDAP helper using rdap.org (no API key needed):

New function: checkRDAPDomain(ctx, domain)

Call: GET https://rdap.org/domain/${domain}

Return ThreatResult:

source: "whois" (or "rdap" but keep consistent with frontend)

data: registrar, creation date, expiry, nameservers, status

isMalicious: usually false (unless you decide to flag suspicious statuses)

Add /domain route handler in the router:

Match:

if (path === "/domain" || path === "/domain/") { ... }

Body: { domain }

Run checks (depending on tier + allowed sources):

if virustotal configured/allowed: checkVirusTotalDomain

always allow rdap: checkRDAPDomain (since it’s free)

Aggregate response matching your UI expectations:

Return shape should mirror the hash normalized style you already do later in the file:

{
  domain,
  isMalicious,
  overallThreatScore,
  sources: {
    virustotal: { found, malicious, details, error?, threatScore? },
    whois: { found, malicious, details, error? }
  },
  checkedAt,
  tier,
  sourcesAvailable
}

Key point: use canonical keys virustotal and whois so the front-end doesn’t play guessing games.

Update “Not found” list to include "/domain".

B) Frontend lib: add scanDomain() and fix URL normalization keys

File: src/lib/threatIntel.ts (you uploaded it)

1) Add:

export async function scanDomain(domain: string)

Call:

POST ${EDGE_FUNCTION_URL}/domain with { domain }

Normalize:

Ensure results keys are canonical: virustotal, whois

Keep raw payload under rawResults if you want.

2) Fix scanURL() normalization so URLResult can actually render

Right now the Edge returns keys like:

virustotal_url

urlhaus_url

urlscan

But your URLResult expects:

virustotal

urlscan

So in scanURL() normalization step, add a mapping like:

virustotal_url → virustotal

urlhaus_url → urlhaus

urlscan stays urlscan

This single change will make URLResult’s resultsData.virustotal start working immediately.